package com.github.alderfurtado.openaikmp

import com.github.alder.furtado.openaikmp.OpenAiKmp
import com.github.alder.furtado.openaikmp.domain.entity.OpenAiInput
import com.github.alder.furtado.openaikmp.domain.entity.OpenAiModel
import kotlinx.coroutines.runBlocking
import kotlin.test.Test
import kotlin.test.assertEquals

class OpenAiKmpTest {

    @Test
    fun test() = runBlocking{
//        val teste = OpenAiKmp().sendContent(""OpenAiInput(openAiModel = OpenAiModel.GPT_4O,"Quem descobriu o Brasil", instruction = "Talks as History teacher"),"token")
//        println(teste.content)
//        assertEquals(false,false)
    }
}